#include <stdio.h>
/**
 * main - program main entry point
 *
 * Description: prints out base 10 digits
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int i;
for(i=0; i<0; i++)
{
print(i);
}
return (0);
}
